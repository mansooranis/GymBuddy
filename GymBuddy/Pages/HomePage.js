import { SafeAreaView, ScrollView, Text } from "react-native";

export default function HomePage(){
    return (
        <SafeAreaView>
            <ScrollView>
                <Text>Hello</Text>
            </ScrollView>
        </SafeAreaView>
    )
}